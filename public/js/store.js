const initialState = {
    playerName: '',
    isAuthenticated: false,
    wsConnected: false,
    players: [],
    messages: [],
    // ... other state
};

export function createStore(initialState) {

} 